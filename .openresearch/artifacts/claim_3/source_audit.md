# Claim 3 source audit

Proposition 3.4 quantifies over any true distribution, sampled corpus, and
partition witnessing exact calibration. From `p(U)>0`, choose unseen `y*` with
`p(y*)>0`. Its partition cell has positive mass, so the coarsened distribution
assigns `g(y*)=p(B*)/|B*|>0`.

