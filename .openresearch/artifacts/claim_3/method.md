# Claim 3 method

The verifier reconstructs the positive-mass witness argument and exhausts every
denominator-three distribution, valid observed subset, and set partition for
universes through size six. The independent check explicitly locates the
positive unseen witness and its cell. The control keeps missing mass positive
but removes exact calibration and must yield `g(U)=0`.

