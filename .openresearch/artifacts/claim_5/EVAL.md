# Claim 5 pre-run evaluation

Status: `BLOCKED` pending supervised execution. Fixed command:
`uv run --frozen python -m repro_campaign.run`; pinned Python 3.13 `uv.lock`.
All cumulative checks and the sharpness control exit nonzero on mismatch.

