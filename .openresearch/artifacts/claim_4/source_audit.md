# Claim 4 source audit

Theorem 4.1 uses the strict interval `delta∈(K/|U|,1)`. This makes
`t=1-K/(delta|U|)` strictly between zero and one. The historical run used the
excluded endpoint, producing the vacuous factor zero.

