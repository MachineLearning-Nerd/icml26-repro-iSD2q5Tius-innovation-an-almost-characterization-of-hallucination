# Claim 2 pre-run evaluation

Status: `BLOCKED` pending supervised execution.

Fixed command: `uv run --frozen python -m repro_campaign.run`.
Environment: Python 3.13 from `uv.lock`. Any failed derivation, enumeration,
independent identity, negative control, or cumulative Claim 1 regression exits
nonzero.

