# Claim 1 evaluation contract

Status before execution: `BLOCKED`.

The fixed campaign command must execute the primary certificate, the independent
checker, and the negative control. Any mismatch exits nonzero. The generated raw
JSON is `.openresearch/artifacts/claim_1/raw_results.json` and the full JSON is
also printed between `OPENRESEARCH_EVIDENCE_BEGIN` and
`OPENRESEARCH_EVIDENCE_END` for local-mode evidence retention.

This file is updated only on a descendant branch after the supervised run has
completed; completed experiment branches remain immutable.

